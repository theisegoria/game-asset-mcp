# Third-party notices

Game Development Studio 1.0.0 is distributed under the MIT License in
[LICENSE](LICENSE).

## Included in the app bundle

The native app is built with Apple's Swift and SwiftUI toolchain and links to
Apple system frameworks supplied by macOS. The Swift package declares no
third-party package dependencies. No provider SDK, game engine, Blender build,
Node.js runtime, or `game-dev` CLI is bundled in the application ZIP.

## Separate products the app can use

The separately installed `game-dev` CLI and its dependencies retain their own
license notices. Optional workflows can also use Blender, Tripo, Leonardo, and
project-specific engines or adapters. Those are independent products governed
by their own licenses, terms, privacy policies, and availability. Their names
describe compatibility only; no affiliation or endorsement is implied.

Generated or user-supplied assets are not relicensed by this notice. The user
is responsible for verifying the rights, provider terms, source provenance,
and target-project requirements for every asset they produce or vendor.

